{% extends "layouts/app.twig" %}


{% block content %}

    <div class="flex flex-grow items-center justify-center">
        <div class="max-w-prose bg-gray-100 rounded shadow-xl px-12 py-8">
            <h1 class="font-medium font-light text-7xl text-gray-700">{{ config('app.name') }}</h1>
            <p class="text-gray-800 text-lg mt-8">
                Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Donec odio. Quisque volutpat mattis eros.
            </p>
            <p class="text-gray-800 text-lg mt-8 mb-4">
                <a href="{{ url_for('register') }}" class="bg-gray-700 text-white p-3 text-lg rounded hover:bg-gray-600">Join Now</a>
            </p>
        </div>
    </div>

{% endblock %}
